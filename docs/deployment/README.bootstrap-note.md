# Z2 Bootstrap forward-port evidence note

This forward-port deliberately composes Bootstrap into the current Manager and current P3 Z2 installer instead of reviving PR #463 wholesale.

Merge claims are limited to software contracts and CI evidence. Real PYNQ-Z2 first-install, reboot persistence and controlled rollback remain separate HIL qualification evidence as defined in `ppu-bootstrap-factory-and-hil.md`.
