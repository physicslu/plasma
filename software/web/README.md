# Plasma PPU Console

React/TypeScript Web Console for one Plasma Programming Unit (PPU). The UI discovers PPU identity and Site topology from the canonical REST status contract, submits one-based Site jobs to the Plasma Web REST Gateway, polls real job/Site state, and sends cancel requests to the backend. Progress is not generated by a browser timer.

## Runtime path

```text
Browser / Plasma PPU Console
        |
        | Web REST API Contract v3
        | Programming Asset
        v
Plasma Web REST Gateway
        |
        | normalize to execution Image
        | Plasma Protocol v3.3 / PLASMA33
        v
Plasma Server
        |
        v
SiteManager / SiteWorker
```

Canonical identity is:

```text
SITE 1 -> site_id = 1
SITE 2 -> site_id = 2
...
SITE N -> site_id = N
```

The current prototype configuration normally enables SITE 1 and SITE 2. Additional Sites remain disabled or absent according to the PPU-reported topology; the Web Console must not hard-code an eight-Site device assumption.

## Requirements

- Node.js version satisfying `package.json` (`>=22.13.0` at the current baseline)
- Plasma Server and Plasma Web REST Gateway from `../python`

The current Gateway is implemented with Python standard-library `ThreadingHTTPServer`. It is not FastAPI and does not use WebSocket.

## Run locally

Start the Python processes first:

```bash
cd ../python
python -m plasma_server.server --config config/plasma.yaml
python -m plasma_web.gateway --host 127.0.0.1 --port 8080
```

Then start the UI in another terminal:

```bash
npm run install:ci
npm run dev
```

The code-level local Gateway port shown above is not the integration-host deployment source of truth. Deployment values are generated by `scripts/plasmactl` and its persistent deployment configuration.

The deployed API Base is supplied through `NEXT_PUBLIC_PLASMA_API_URL`. Browser storage may hold an operator-selected API Base, but it must not become authoritative for PPU inventory or Site capability.

## Programming data boundary

REST v3 uses **Programming Asset** as the browser-facing source/input abstraction. Current Image upload fields include `asset_name`, `asset_type`, `asset_format`, `asset_size`, and `asset_sha256`. Program/Verify resolve the selected Image Asset into a Normalized Image before Protocol v3.3 execution.

Current runtime implements only `asset_type=image` + `asset_format=binary` normalization. Declared future formats/types must not be presented as supported parsers until implemented.

Serial Number is a distinct Asset type for per-device identity. It is not a security key and is not directly normalized into a Program Image.

## Job behavior

- Canonical Web requests send one-based `site_id` only.
- `program` means write only; a full programming flow is composed as `erase -> program -> verify` when selected.
- Batch operation checkboxes may select any subset of Erase / Program / Verify / Read.
- Selected Sites execute independently/concurrently; one Site must not wait for an unrelated Site unless a real shared resource requires synchronization.
- Per-Site cancellation must not cancel unrelated Sites.
- Current Web/REST runtime does not maintain retired Programmer/Channel/Firmware compatibility aliases.

## Checks

```bash
npm run lint
npm test
npm run validate:artifact
```

Browser-level validation lives under `e2e/` and includes Playwright functional tests plus deterministic Visual Regression scenarios. Mock CD Browser Runtime Acceptance additionally validates the Web Console against a real Gateway/Provider/Server Mock stack rather than replacing the REST API with browser route mocks.
