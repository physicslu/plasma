# STM32F103CB validation checkpoint

This file intentionally contains no validation result. The authoritative acceptance signal is the GitHub Actions `validate-device-catalog` check on the Phase 2.2 pull request.

It exists only to make the research boundary explicit: exact ST commercial evidence is collected first, then deterministic Plasma/OpenOCD mapping is accepted only if the fail-closed validator passes in CI.
