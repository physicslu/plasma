name: Device catalog Phase 4.2E0 P-option routing apply

on:
  push:
    branches:
      - agent/device-catalog-phase4.2e0-f4-p-option-routing

permissions:
  contents: write

jobs:
  apply:
    if: github.actor == 'physicslu'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/device-catalog-phase4.2e0-f4-p-option-routing
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply bounded routing normalization
        run: python data/device-catalog/research/apply_stm32f4_phase4_2e0_p_option_routing.py
      - name: Validate bounded routing normalization
        run: |
          git diff --check
          python data/device-catalog/research/test_stm32f4_phase4_2e0_p_option_routing.py
      - name: Commit bounded policy delta
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/device-catalog/research/stm32f4_admission_policy.py \
                  data/device-catalog/research/test_stm32f4_phase4_2e0_p_option_routing.py
          git diff --cached --check
          git commit -m 'device-catalog: normalize STM32F412 P option for routing'
          git push origin HEAD:agent/device-catalog-phase4.2e0-f4-p-option-routing
