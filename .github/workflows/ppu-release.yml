name: PPU release artifact

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/ppu-release.yml"
      - "scripts/ppu-runtime.py"
      - "scripts/ppu-release.py"
      - "scripts/product-release.py"
      - "scripts/ppu-armv7-runtime-acceptance.py"
      - "scripts/ppu-armv7-runtime-lab.py"
      - "scripts/ppu-network-phase1-acceptance.py"
      - "scripts/ppu-network-phase2-acceptance.py"
      - "scripts/virtual-ppu-network-lab.py"
      - "scripts/static-ipv4-fault-injection-lab.py"
      - "scripts/static-ipv4-fault-injection-repeatability.py"
      - "scripts/private-ppu-evidence-verifier.py"
      - "scripts/static-ipv4-fault-helper.py"
      - "scripts/manager-network-commissioning-crash-injector.py"
      - "release/**"
      - "data/device-catalog/production/**"
      - "software/python/pyproject.toml"
      - "software/python/plasma_core/**"
      - "software/python/plasma_interfaces/**"
      - "software/python/plasma_handlers/**"
      - "software/python/plasma_server/**"
      - "software/python/plasma_client/**"
      - "software/python/plasma_manager/**"
      - "software/python/plasma_web/**"
      - "software/python/tests/test_ppu_runtime.py"
      - "software/python/tests/test_ppu_armv7_runtime_acceptance.py"
      - "software/python/tests/test_ppu_armv7_runtime_lab.py"
      - "software/python/tests/test_ppu_network_phase1_acceptance.py"
      - "software/python/tests/test_ppu_network_activation.py"
      - "software/python/tests/test_ppu_network_activation_rest.py"
      - "software/python/tests/test_ppu_network_activation_security.py"
      - "software/python/tests/test_virtual_ppu_network_lab.py"
      - "software/python/tests/test_static_ipv4_fault_injection_lab.py"
      - "software/python/tests/test_ppu_release.py"
      - "software/python/tests/test_product_release.py"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-ppu-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PPU build and test dependencies
        run: python -m pip install ./software/python pytest

      - name: Run PPU packaging regression tests
        run: >-
          python -m pytest -q
          software/python/tests/test_ppu_runtime.py
          software/python/tests/test_ppu_armv7_runtime_acceptance.py
          software/python/tests/test_ppu_armv7_runtime_lab.py
          software/python/tests/test_ppu_network_phase1_acceptance.py
          software/python/tests/test_ppu_network_activation.py
          software/python/tests/test_ppu_network_activation_rest.py
          software/python/tests/test_ppu_network_activation_security.py
          software/python/tests/test_virtual_ppu_network_lab.py
          software/python/tests/test_static_ipv4_fault_injection_lab.py
          software/python/tests/test_ppu_release.py
          software/python/tests/test_product_release.py

      - name: Build and validate PPU runtime
        shell: bash
        run: |
          set -euo pipefail
          runtime="$RUNNER_TEMP/plasma-ppu-runtime"
          python scripts/ppu-runtime.py build --output-dir "$runtime"
          python scripts/ppu-runtime.py validate "$runtime"

      - name: Build canonical linux-armv7l PPU release
        id: release
        shell: bash
        run: |
          set -euo pipefail
          runtime="$RUNNER_TEMP/plasma-ppu-runtime"
          output="$RUNNER_TEMP/plasma-ppu-release"
          sha="$(git rev-parse HEAD)"
          version="$(python -c 'import json; print(json.load(open("release/product.json"))["product_version"])')"

          python scripts/ppu-release.py \
            --runtime-dir "$runtime" \
            --output-dir "$output" \
            --git-sha "$sha"

          archive="$output/plasma-ppu-$version-linux-armv7l.tar.gz"
          sidecar="$archive.sha256"
          test -f "$archive"
          test -f "$sidecar"

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "archive=$archive" >> "$GITHUB_OUTPUT"
          echo "sidecar=$sidecar" >> "$GITHUB_OUTPUT"

      - name: Verify release and closed hardware boundary from clean extraction
        shell: bash
        env:
          ARCHIVE: ${{ steps.release.outputs.archive }}
        run: |
          set -euo pipefail
          clean="$RUNNER_TEMP/plasma-ppu-verify"

          python scripts/product-release.py verify \
            "$ARCHIVE" \
            --expect-role ppu \
            --expect-platform linux \
            --expect-architecture armv7l \
            --expect-version "${{ steps.release.outputs.version }}" \
            --extract-to "$clean"

          python scripts/ppu-runtime.py validate "$clean/plasma-release/runtime"

          python - "$clean/plasma-release/runtime/ppu-runtime.json" <<'PY'
          import json
          import sys

          manifest = json.load(open(sys.argv[1], encoding="utf-8"))
          expected = {
              "loads_fpga": False,
              "accesses_pl": False,
              "changes_target_power": False,
              "programs_real_ic": False,
          }
          if manifest.get("hardware_boundary") != expected:
              raise SystemExit(f"hardware boundary is not closed: {manifest.get('hardware_boundary')!r}")
          PY

      - name: Verify detached SHA-256 sidecar
        shell: bash
        env:
          ARCHIVE: ${{ steps.release.outputs.archive }}
          SIDECAR: ${{ steps.release.outputs.sidecar }}
        run: |
          set -euo pipefail
          cd "$(dirname "$ARCHIVE")"
          sha256sum --check "$(basename "$SIDECAR")"

      - name: Set up QEMU for ARMv7 runtime acceptance
        uses: docker/setup-qemu-action@v4
        with:
          image: docker.io/tonistiigi/binfmt@sha256:400a4873b838d1b89194d982c45e5fb3cda4593fbfd7e08a02e76b03b21166f0
          platforms: arm

      - name: Run PPU release under ARMv7 userspace
        shell: bash
        run: |
          set -euo pipefail
          runtime="$RUNNER_TEMP/plasma-ppu-verify/plasma-release/runtime"
          work="$RUNNER_TEMP/plasma-ppu-armv7-work"
          test -f "$runtime/ppu/ppu.pyz"
          mkdir -p "$work"

          docker run --rm \
            --platform linux/arm/v7 \
            --volume "$runtime:/runtime:ro" \
            --volume "$PWD/scripts/ppu-armv7-runtime-acceptance.py:/acceptance.py:ro" \
            --volume "$work:/work" \
            arm32v7/python@sha256:45eb5cbc14fe248e7598eb23a5a61424d44e556aed3efa955dfab2ac9a67d91c \
            python /acceptance.py \
              --runtime-dir /runtime \
              --work-dir /work

      - name: Run one-command PPU Network Phase 1 acceptance
        shell: bash
        run: |
          set -euo pipefail
          python scripts/ppu-network-phase1-acceptance.py \
            --work-dir "$RUNNER_TEMP/plasma-ppu-network-phase1" \
            --report "$RUNNER_TEMP/plasma-ppu-network-phase1.json"

      - name: Run one-command PPU Network Phase 2 acceptance
        shell: bash
        run: |
          set -euo pipefail
          python scripts/ppu-network-phase2-acceptance.py \
            --work-dir "$RUNNER_TEMP/plasma-ppu-network-phase2" \
            --report "$RUNNER_TEMP/plasma-ppu-network-phase2.json"

      - name: Run Virtual PPU Network Lab with production Manager
        shell: bash
        run: |
          set -euo pipefail
          python scripts/virtual-ppu-network-lab.py \
            --runtime-dir "$RUNNER_TEMP/plasma-ppu-verify/plasma-release/runtime" \
            --work-dir "$RUNNER_TEMP/plasma-virtual-ppu-network-lab" \
            --report "$RUNNER_TEMP/plasma-virtual-ppu-network-lab.json"

      - name: Run Static IPv4 repeatability and privilege-parity gate
        shell: bash
        run: |
          set -euo pipefail
          python scripts/static-ipv4-fault-injection-repeatability.py \
            --runtime-dir "$RUNNER_TEMP/plasma-ppu-verify/plasma-release/runtime" \
            --work-dir "$RUNNER_TEMP/plasma-static-ipv4-fault-injection" \
            --report "$RUNNER_TEMP/plasma-static-ipv4-fault-injection-repeatability.json"

      - name: Upload PPU release artifact
        uses: actions/upload-artifact@v4
        with:
          name: plasma-ppu-linux-armv7l-${{ github.sha }}
          path: |
            ${{ steps.release.outputs.archive }}
            ${{ steps.release.outputs.sidecar }}
          if-no-files-found: error
          retention-days: 14
