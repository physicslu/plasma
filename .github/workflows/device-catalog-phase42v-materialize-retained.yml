name: Device catalog Phase 4.2V materialize retained evidence
on:
  push:
    branches: [agent/device-catalog-phase4.2v-h-admission-batch2]
    paths: [".github/workflows/device-catalog-phase42v-materialize-retained.yml"]
  workflow_dispatch:
permissions: {actions: read, contents: write}
jobs:
  materialize:
    if: github.actor == 'physicslu'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: agent/device-catalog-phase4.2v-h-admission-batch2, fetch-depth: 0}
      - uses: actions/download-artifact@v4
        with: {name: phase42v-h-batch2-retained, path: /tmp/phase42v, repository: physicslu/plasma, run-id: 33824977123, github-token: "${{ secrets.GITHUB_TOKEN }}"}
      - name: Materialize exact retained evidence
        run: |
          set -euo pipefail
          dst=data/device-catalog/research/evidence/stm32f4-phase4.2v-h-admission-batch2-live-2026-09-03
          test -f /tmp/phase42v/retained/manifest.json
          test "$(python -c "import json; print(json.load(open('/tmp/phase42v/retained/manifest.json'))['evidence_id'])")" = "stm32f4-phase4.2v-h-admission-batch2-2026-09-03-retained-20260904T011528Z-cd38d54"
          test ! -e "$dst"
          mkdir -p "$dst"
          cp -a /tmp/phase42v/retained/. "$dst/"
          python data/device-catalog/research/validate_stm32f4_retained_evidence.py --evidence-dir "$dst" --baseline data/device-catalog/research/stm32f4-phase4.2v-h-admission-batch2-baseline.json
      - name: Commit retained evidence
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/device-catalog/research/evidence/stm32f4-phase4.2v-h-admission-batch2-live-2026-09-03
          git commit -m 'device-catalog: retain Phase 4.2V official ST evidence'
          git push origin HEAD:agent/device-catalog-phase4.2v-h-admission-batch2
