name: Device catalog Phase 4.3E STM32F2 discovery

on:
  push:
    branches: [agent/device-catalog-phase43e-stm32f2-batch2]
    paths:
      - ".github/workflows/device-catalog-phase43e-discovery.yml"
      - "data/device-catalog/research/stm32f2_phase4_3e_discovery.py"
      - "data/device-catalog/research/stm32f2-phase4.3e-discovery-manifest.json"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover:
    if: github.actor == 'physicslu'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate Phase 4.3E discovery contract
        run: python data/device-catalog/research/test_stm32f2_phase4_3e_discovery.py
      - name: Install bounded browser runtime
        run: |
          python -m pip install playwright==1.62.0
          python -m playwright install --with-deps chromium
      - name: Acquire official ST STM32F2 discovery evidence
        run: |
          mkdir -p /tmp/phase43e
          xvfb-run -a python data/device-catalog/research/stm32f2_phase4_3e_discovery.py \
            --output /tmp/phase43e/pilot-summary.json \
            --timeout 75 \
            --delay 2
          cd /tmp/phase43e
          sha256sum pilot-summary.json > SHA256SUMS
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: phase43e-stm32f2-discovery
          path: /tmp/phase43e
          if-no-files-found: error
          retention-days: 30
