name: STM32F4 Phase 3.3 scaleout live evidence

on:
  pull_request:
    paths:
      - "data/device-catalog/research/stm32f4-phase3.3-*"
      - "data/device-catalog/research/stm32f4_admission.py"
      - "data/device-catalog/research/stm32f4_admission_policy.py"
      - "data/device-catalog/research/stm32f4_acquisition_pilot.py"
      - "data/device-catalog/research/run_stm32f4_browser_pilot.py"
      - ".github/workflows/stm32f4-phase3.3-scaleout-live-evidence.yml"

permissions:
  contents: read

jobs:
  acquire-live-evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install pinned Playwright Chromium
        run: |
          python -m pip install "playwright==1.62.0"
          python -m playwright install --with-deps chromium
      - name: Resolve executed Git SHA
        run: echo "EXECUTED_GIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
      - name: Run headed Chromium control and scaleout batch
        run: |
          mkdir -p phase3.3-live
          xvfb-run -a python data/device-catalog/research/run_stm32f4_browser_pilot.py \
            --manifest data/device-catalog/research/stm32f4-phase3.3-scaleout-batch1-manifest.json \
            --scope control --headed \
            --output phase3.3-live/control-summary.json
          xvfb-run -a python data/device-catalog/research/run_stm32f4_browser_pilot.py \
            --manifest data/device-catalog/research/stm32f4-phase3.3-scaleout-batch1-manifest.json \
            --scope pilot --headed \
            --output phase3.3-live/pilot-summary.json
      - name: Evaluate and retain evidence
        run: |
          python data/device-catalog/research/evaluate_stm32f4_pilot.py \
            --summary phase3.3-live/pilot-summary.json \
            --baseline data/device-catalog/research/stm32f4-phase3.3-scaleout-batch1-baseline.json \
            --repository physicslu/plasma \
            --git-sha "$EXECUTED_GIT_SHA" \
            --output phase3.3-live/evaluation.json
          python data/device-catalog/research/retain_stm32f4_browser_evidence.py \
            --control phase3.3-live/control-summary.json \
            --pilot phase3.3-live/pilot-summary.json \
            --evaluation phase3.3-live/evaluation.json \
            --baseline data/device-catalog/research/stm32f4-phase3.3-scaleout-batch1-baseline.json \
            --output-dir phase3.3-live/evidence
      - name: Build read-only admission plan
        run: |
          python data/device-catalog/research/stm32f4_admission.py \
            --evidence-dir phase3.3-live/evidence \
            --baseline data/device-catalog/research/stm32f4-phase3.3-scaleout-batch1-baseline.json \
            --catalog data/device-catalog/research/openocd-parts-canonical.csv \
            --canonical data/device-catalog/research/stm32f4-commercial-icpn.csv \
            --output phase3.3-live/admission-plan.json
      - uses: actions/upload-artifact@v4
        with:
          name: stm32f4-phase3.3-scaleout-live-evidence
          path: phase3.3-live/
          if-no-files-found: error
