name: Device catalog Phase 4.2B STM32F410CB admission proposal

on:
  pull_request:

permissions:
  contents: read

jobs:
  propose-stm32f410cb:
    if: >-
      github.actor == 'physicslu' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/device-catalog-phase4.2b-f410cb-admission'
    runs-on: ubuntu-latest
    env:
      RESEARCH: data/device-catalog/research
      EVIDENCE_DIR: data/device-catalog/research/evidence/stm32f4-phase4.2b-f410cb-admission-live-2026-09-02
      BASELINE: data/device-catalog/research/stm32f4-phase4.2b-f410cb-admission-baseline.json
      CANONICAL: data/device-catalog/research/stm32f4-commercial-icpn.csv
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate retained evidence offline
        run: |
          python "${RESEARCH}/validate_stm32f4_retained_evidence.py" \
            --evidence-dir "${EVIDENCE_DIR}" \
            --baseline "${BASELINE}"
      - name: Build deterministic read-only admission proposal
        run: |
          set -euo pipefail
          mkdir -p /tmp/phase42b-f410cb-proposal
          python "${RESEARCH}/stm32f4_admission.py" \
            --evidence-dir "${EVIDENCE_DIR}" \
            --baseline "${BASELINE}" \
            --catalog "${RESEARCH}/openocd-parts-canonical.csv" \
            --canonical "${CANONICAL}" \
            --admit-base STM32F410CB \
            --output /tmp/phase42b-f410cb-proposal/admission-plan.json
          python - <<'PY'
          import hashlib
          import json
          import shutil
          import sys
          from pathlib import Path

          research = Path('data/device-catalog/research')
          sys.path.insert(0, str(research))
          from device_catalog_admission_framework import write_canonical_dataset

          out = Path('/tmp/phase42b-f410cb-proposal')
          plan_path = out / 'admission-plan.json'
          plan = json.loads(plan_path.read_text(encoding='utf-8'))
          expected_counts = {
              'admit': 5,
              'already_present': 0,
              'manual_review_required': 0,
              'reject': 0,
          }
          assert plan['candidate_count'] == 5
          assert plan['decision_counts'] == expected_counts
          assert plan['conflicts'] == 0
          assert plan['issues'] == []
          assert plan['canonical_rows_before'] == 194
          assert plan['inputs']['admission_base_devices'] == ['STM32F410CB']

          source = research / 'stm32f4-commercial-icpn.csv'
          proposed = out / 'stm32f4-commercial-icpn.proposed.csv'
          shutil.copyfile(source, proposed)
          write_result = write_canonical_dataset(plan=plan, canonical_path=proposed)
          assert write_result['status'] == 'written'
          assert write_result['rows_before'] == 194
          assert write_result['rows_after'] == 199
          assert len(write_result['added']) == 5
          expected = {
              'STM32F410CBT3',
              'STM32F410CBT6',
              'STM32F410CBU3',
              'STM32F410CBU3TR',
              'STM32F410CBU6',
          }
          assert set(write_result['added']) == expected

          def sha(path: Path) -> str:
              return hashlib.sha256(path.read_bytes()).hexdigest()

          payload = proposed.read_bytes()
          git_blob = hashlib.sha1(f'blob {len(payload)}'.encode() + bytes([0]) + payload).hexdigest()
          summary = {
              'schema_version': 1,
              'proposal_head_sha': '${{ github.event.pull_request.head.sha }}',
              'evidence_id': plan['evidence_id'],
              'candidate_count': plan['candidate_count'],
              'decision_counts': plan['decision_counts'],
              'conflicts': plan['conflicts'],
              'issues': plan['issues'],
              'canonical_rows_before': plan['canonical_rows_before'],
              'canonical_rows_after': write_result['rows_after'],
              'added': write_result['added'],
              'admission_plan_sha256': sha(plan_path),
              'proposed_csv_sha256': sha(proposed),
              'proposed_csv_git_blob': git_blob,
              'canonical_dataset_written': False,
          }
          (out / 'proposal-summary.json').write_text(
              json.dumps(summary, indent=2, sort_keys=True) + '\n', encoding='utf-8'
          )
          print(json.dumps(summary, indent=2, sort_keys=True))
          PY
      - name: Upload immutable proposal
        uses: actions/upload-artifact@v4
        with:
          name: phase42b-stm32f410cb-admission-proposal
          path: /tmp/phase42b-f410cb-proposal
          if-no-files-found: error
          retention-days: 30
