name: STM32U5 metadata authority delta validation

on:
  pull_request:
    paths:
      - 'data/device-catalog/research/stm32u5-metadata-authority-delta-resolution.json'
      - 'data/device-catalog/research/validate_stm32u5_metadata_authority_delta_resolution.py'
      - 'data/device-catalog/research/device-catalog-stm32u5-metadata-authority-delta-resolution.md'
      - 'data/device-catalog/research/stm32u5-metadata-policy-baseline.json'
      - 'data/device-catalog/research/stm32u5-metadata-authority.json'
      - 'data/device-catalog/research/validate_stm32u5_metadata_policy.py'
      - '.github/workflows/device-catalog-stm32u5-metadata-authority-delta-resolution-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Replay merged STM32U5 metadata policy
        run: python data/device-catalog/research/validate_stm32u5_metadata_policy.py
      - name: Validate STM32U5 metadata authority delta disposition
        run: python data/device-catalog/research/validate_stm32u5_metadata_authority_delta_resolution.py
      - name: Enforce zero Production writes
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          changed="$(git diff --name-only "origin/${{ github.base_ref }}"...HEAD -- data/device-catalog/production/)"
          test -z "$changed" || { echo "Production writes are forbidden in STM32U5 metadata authority delta resolution:"; echo "$changed"; exit 1; }
